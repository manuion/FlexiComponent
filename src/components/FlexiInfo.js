import React from 'react';

const FlexiInfo = ({ name, place }) => {
    return(
        <div>
            <p>Entered name : {name}</p>
            <p>Selected place : {place}</p>
        </div>
    );
}

export default FlexiInfo;
